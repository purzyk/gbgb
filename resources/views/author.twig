{% extends "base.twig" %}

{% block content %}
    <h1 class="title is-2">{{ author }}</h1>
    {% for post in posts %}
        {{ post.title }}
        <hr />
    {% endfor %}

    {% if posts.pagination.pages %}
        {% include 'components/pagination.twig' %}
    {% endif %}
{% endblock %}
